## Java Scriptとは

**1. 基本的なこと**
- Webアプリ開発にて必須の存在
- 現在、ほぼ全てのサイトで使用されている

**2. プログラムの実行**
```script
console.log("Hello World");
```
【出力結果】
>_ コンソール
Hello world

メモ：
- コンソールという画面は出力結果を出してくれる
- "console.log(~);"というコードを書くと、()の~という文字がコンソールに出力される

